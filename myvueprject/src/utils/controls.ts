import { reactive } from "vue";

const controlObj = reactive({
    // 
    isImg: false,

    isshowlogin:false,

    // 遮罩层
    isShow:false,

    // 人物显示
    isRole:false
})

export default controlObj;